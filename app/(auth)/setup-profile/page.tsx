import { AuthSetupProfile } from "@/components/features/auth/auth-setup-profile";

export default function SetupProfilePage() {
	return (
		<div className="min-h-screen bg-background">
			<AuthSetupProfile />
		</div>
	);
}
